apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'Chobob City'
            keyPassword 'chobob_city_00000000'
            storeFile file('ChobobCity.jks')
            storePassword 'chobob_city_00000000'
        }
    }
    compileSdkVersion 22
    buildToolsVersion "22.0.1"
    defaultConfig {
        applicationId "kr.nogcha.sejongbus"
        minSdkVersion 14
        targetSdkVersion 22
        versionCode 2
        versionName "2.0.0"
        signingConfig signingConfigs.config
    }
    buildTypes {
        release {
            minifyEnabled true
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    productFlavors {
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:22.0.+'
    compile 'com.android.support:support-v13:22.0.+'
    compile 'com.google.android.gms:play-services-location:6.5.87'
    compile 'com.squareup.okhttp:okhttp-urlconnection:2.4.0'
    compile 'com.squareup.okhttp:okhttp:2.4.0'
    compile 'com.squareup.retrofit:retrofit:1.9.0'
    compile 'org.droidparts:droidparts:2.7.5'
}
